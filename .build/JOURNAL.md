# BUILD JOURNAL

2026-09-17 | setup | Read all 9 spec files in docs/spec/ (1315 lines). Duplicate copy confirmed byte-identical.
2026-09-17 | setup | Owner decisions: rename folder to delphi-team; move spec to docs/spec/ + delete duplicate; survey local Pythia install after updating it to latest.
2026-09-17 | setup | Folder renamed pythia-agents-team -> delphi-team. `git init` done. `.build/` created.
2026-09-17 | P0 | Toolchain verified: claude 2.1.274, node 22.16.0, pnpm 10.33.2, git 2.54.0, uv 0.11.23, wt present, tmux + pipx absent.
2026-09-17 | P0 | `claude --help` confirms --agent/--agents/--bg/--model/--effort/--name/--resume/--autocompact/--worktree/--tmux/--permission-mode and subcommands agents|attach|logs|stop|rm|respawn|doctor|plugin|mcp.
2026-09-17 | P0 | `claude agents --help` confirms `--json` (active sessions, no TTY needed), `--all` (include completed), `--cwd` filter.
2026-09-17 | P0 | `~/.claude/keybindings.json` does not exist -> Alt+V binding is at product default, needs doc confirmation.
2026-09-17 | P0 | Local spike: SessionStart hook fires before authentication and receives session_id, transcript_path, cwd, agent_type, hook_event_name, source. Confirmed source=startup and source=resume, and that agent_type is absent without --agent.
2026-09-17 | P0 | Local spike: a hook exiting 1 with malformed stdout did not stop the session -> fail-open confirmed.
2026-09-17 | P0 | BLOCKER: every spawned `claude` CLI process fails with "OAuth session expired and could not be refreshed". Model-side spikes cannot run until the owner runs `claude auth login`.
2026-09-17 | P0 | Four doc-research subagents returned: agent teams + messaging, hooks + skills + memory, sessions + CLI + images + plugins, packaging.
2026-09-17 | P0 | Pythia surveyed: pythia-plsql 0.14.2 (already latest, no update needed). It is a CLI, not an MCP server for Claude Code. Facts recorded in docs/research/pythia-capability-notes.md.
2026-09-17 | P0 | Wrote .build/VERIFY.md, .build/CONFLICTS.md (C-001..C-010) and docs/research/claude-code-capabilities.md.
2026-09-17 | P0 | ADR-0003 (bun build --compile) and ADR-0004 (drop the `dt` alias) recorded.
2026-09-17 | P0 | Owner decision: finish the blocked spikes before Phase 1 - they will run `claude auth login` and signal.
2026-09-17 | P0 | Owner decision: the Python wheel fails loudly instead of falling back to npx (ADR-0005, amends PACKAGING_SPEC section 1, recorded as C-011).
2026-09-17 | P0 | Owner re-authenticated the CLI. Spikes unblocked.
2026-09-17 | P0 | Spike 1 PASS: SessionStart additionalContext reached the model (DELTA-ALPHA-7X9) and the --agent definition body was loaded as the main session (ZEBRA-QUARTZ).
2026-09-17 | P0 | Spike 2 PASS: `claude --bg --agent --model --effort --name` launched from the Bash tool inside a live session; ran to state=done. `sessions` mode and R17 unblocked. Docs never address this, so doctor will probe it at runtime.
2026-09-17 | P0 | Spike 3 PASS: `claude agents --json` field union across six live entries = cwd,id,kind,name,pid,sessionId,startedAt,state,status (+waitingFor). No model, no agent - C-007 confirmed empirically.
2026-09-17 | P0 | Spike 4 PASS: a Desktop session discovered a CLI-spawned background session via ListAgents and SendMessage was queued to it. Receiver then sat at waitingFor="permission prompt", state="blocked" - a held message, not a delivered one.
2026-09-17 | P0 | New findings: ListAgents has a third kind `Remote Control` (40 of 45 peers, offline) -> C-012. Permission-block signal is readable from claude agents --json -> C-013. `claude logs` returns raw ANSI, unparseable.
2026-09-17 | P0 | Spikes 5 (teams, 2 teammates) and 6 (VS Code extension inbox) deferred to Phase 3 and Phase 5 - neither is needed before the phase that builds the feature it tests.
2026-09-17 | P0 | All spike background sessions stopped and removed; zero leftovers verified.
2026-09-17 | P0 | PHASE 0 DONE. Waiting for the owner to approve Phase 1.
2026-09-17 | P1 | Installed bun 1.4.2 and pipx 1.17.3 (owner approved).
2026-09-17 | P1 | Monorepo scaffolded: packages/{core,cli,templates}, python/, scripts/, .github/. TS strict ESM, vitest 5, biome 2.5.14, changesets 3, commander 15, typescript 7.0.2.
2026-09-17 | P1 | TypeScript 7 breaks tsup --dts via rollup-plugin-dts. core switched to plain tsc (ADR-0006); cli keeps tsup for bundling + version inlining.
2026-09-17 | P1 | Fixed: @delphi-team/core was briefly a runtime dependency of the published package - it is inlined by tsup and private, so shipping it would make the tarball uninstallable. Guard test added (ADR-0007).
2026-09-17 | P1 | pnpm check green: biome clean, tsc clean, build ok, 12 tests pass, core coverage 100% (threshold 80%).
2026-09-17 | P1 | npm pack -> npm i -g -> `delphi --version` = 0.1.0 and `delphi-team --version` = 0.1.0.
2026-09-17 | P1 | bun build --compile -> 83 MB Windows x64 binary, runs standalone, prints 0.1.0. Matches the 60-85 MB estimate from Phase 0.
2026-09-17 | P1 | Wheel built: delphi_team-0.1.0-py3-none-win_amd64.whl, Root-Is-Purelib: false, binary inside. pipx install -> `delphi --version` = 0.1.0.
2026-09-17 | P1 | PACKAGING_SPEC section 8 acceptance: npm and PyPI --version and --help output are byte-identical. Exit code propagates through the Windows shim.
2026-09-17 | P1 | ADR-0005 verified: with the binary removed, the shim refuses with exit 1 and an npm install hint instead of shelling out to npx.
2026-09-17 | P1 | sync-version --check proven to detect drift (exit 1), repair it, and return clean.
2026-09-17 | P1 | Uninstalled the pipx build afterwards so a frozen 0.1.0 binary cannot shadow development builds.
2026-09-17 | P1 | Read-only review found two real defects in the Python shim: a present-but-unrunnable binary raised an unhandled OSError traceback instead of the crafted message, and Ctrl+C on Windows surfaced as an uncaught KeyboardInterrupt. Both fixed; python/test_shim.py added (plain asserts, no framework) and wired into ci.yml.
2026-09-17 | P1 | Owner approved creating the public GitHub repository now, and splitting Phase 2 into three reported stages.
2026-09-17 | P1 | Public repo created at github.com/thaildhe172591/delphi-team and pushed. First CI run: 5 of 6 jobs green.
2026-09-17 | P1 | CI failure was real and useful: the unicode-path job checks out below the workspace root, so pnpm/action-setup found no package.json to read `packageManager` from. Fixed by pointing package_json_file at the checkout.
2026-09-17 | P2a | Core built: zod schemas (config, role, capability, team, story, board, project index), role build with marked blocks and the documented frontmatter precedence, ledger (paths, locking store, journal, board with transition rules), and the claude adapter.
2026-09-17 | P2a | The concurrency test earned its place twice: it found that the retry budget starved writers, and then that a retry-based file lock has no fairness at all. Fixed properly with an in-process queue (ADR-0008) rather than by raising retries.
2026-09-17 | P2a | 86 tests pass. packages/core coverage: statements 92%, branches 85%, functions 94%, lines 93% - all above the 80% threshold.
2026-09-17 | P2a | CI caught two real defects the local run could not. (1) execa 10 and commander 15 both require Node >= 22, silently breaking the `engines: >=20` floor the spec fixes; the node-20 job failed with `TEXT_ENCODINGS.union is not a function`. Pinned execa ^9.6.1 and commander ^14.0.3, which are the newest lines that still support Node 20. (2) The adapter test asserted `claude` was on PATH, which no CI runner has; it now spawns node as a stand-in to exercise the same code, and the real-Claude check reports a skip instead of failing.
2026-09-17 | P2b | PROTOCOL.md written: 96 lines, all 12 mandatory sections (ORCHESTRATION_SPEC section 11 caps it at 150).
2026-09-17 | P2b | All templates written: 10 standard roles, 5 optional roles, 8 capability packs (including the Pythia pack filled from the real install), 5 team templates, PROTOCOL.md, 9 ledger templates, 4 artifact templates, and the commented sample config. 43 files.
2026-09-17 | P2b | Templates are embedded into a generated TypeScript module rather than read from disk, because a bun-compiled binary has no template directory. One mechanism serves npm, the wheels and the plugin. The generated file is committed and CI fails when it is stale - proven to bite.
2026-09-17 | P2b | The template test suite found two real defects the type checker could not: an unquoted colon in the reviewer description broke its YAML frontmatter outright, and db-engineer and reviewer were each missing a section ROLES_SPEC section 1 requires. Both fixed.
2026-09-17 | P2b | 186 tests pass. Every shipped template is parsed and validated, every role is checked for its nine sections, its length, and that it pins no model; every capability for its guard rails and evidence section; every team for real seats and a sane active limit; and every template is swept for anything that suggests bypassing permissions.
2026-09-17 | P2b | Owner raised the Node floor to >=22 (ADR-0009), so execa and commander return to their current releases and the CI floor job now guards Node 22. The spec line in WORKFLOW section 2 is annotated in place rather than silently rewritten.
2026-09-17 | P2c | CLI built: init, doctor, role/capability/team, project/resume/state/journal/checkpoint, story/task/report, seat/start/dispatch/snap, hook. Judgement lives in core as pure functions; the CLI gathers facts and prints.
2026-09-17 | P2c | Three defects found by running it rather than by reading it. (1) commander `.command()` returns the subcommand, so `story` and `team` were never registered and their children leaked to the top level. (2) proper-lockfile is CommonJS and was bundled into the ESM output because the CLI did not declare core dependencies, so the binary died on its first require(). (3) A hook payload cwd that Node cannot resolve made SessionStart inject nothing, silently. All three now have tests.
2026-09-17 | P2c | Dogfooded: `delphi init` on this repository, then a real project ledger with the remaining phases as stories. init is idempotent, doctor reports clean, and the board gates refuse `ready` without acceptance criteria and `done` without a report.
2026-09-17 | P2c | 244 tests pass.
2026-09-17 | P2c | CI found an ordering bug the local machine hid: `pnpm check` ran typecheck before build, so the CLI typechecked against core generated .d.ts files that do not exist on a clean checkout. Locally a stale dist masked it. The order is now lint -> build -> typecheck -> test, and it was verified from no dist at all.
2026-09-17 | P3a | Dispatch decisions moved into core as pure functions: choosing a mode from the environment, and planning which seats start. The pre-spawn checks refuse two stories claiming the same files, two seats owning one path, a story with no acceptance criteria, and more active seats than the team can coordinate.
2026-09-17 | P3a | Commands added: dept up/status/down, shift end, handoff new/list, inbox, next.
2026-09-17 | P3a | Two defects found by running it. A story with a bare `handoff_to:` parsed as null and failed the whole schema, and the CLI then reported "no acceptance criteria" - the symptom, not the cause. Making the reader say what was actually wrong then exposed the real root cause underneath: YAML turns an unquoted ISO timestamp into a Date, which the schema rejected. Every story would have hit it. Fixed in the schema, which fixes it for every reader.
2026-09-17 | P3a | Spike script written for the owner to run: scripts/spike-agent-teams.mjs. Agent Teams needs an interactive CLI session, so it cannot be driven from a tool call.
2026-09-17 | P3a | 276 tests pass.
2026-09-17 | P3b | Terminal surfaces added: one wt invocation with panes chained by a bare `;`, or a tmux session created, filled, tiled and attached. Built as data rather than executed directly, so `dept up --surface` prints the exact command first - it opens windows on someone screen and the wt form has not been run end to end.
2026-09-17 | P3b | `delphi watch` added: read-only, board and live seats side by side so a disagreement between them is visible. `--follow` redraws without pulling in a dependency.
2026-09-17 | P3b | Agent Teams spike run by the owner. A team was created but with only the lead as a member; the two named agents ran as background subagents. TaskCreated/TaskCompleted/TeammateIdle never fired. The task tools did not exist at all, so there was no shared list to gate. Per-agent model worked. A subagent DID receive a skill from its definition, which the docs say a teammate never does.
2026-09-17 | P3b | New payload fields the documentation did not mention: SessionStart carries scratchpad_dir and model; SubagentStart carries prompt_id and a cwd of <project>/.claude/agents; SubagentStop carries permission_mode, agent_transcript_path, last_assistant_message, background_tasks and session_crons.
2026-09-17 | P3b | Design changed accordingly: delphi installs a SubagentStop hook, which is the event that actually arrives, and records each seat closing line in the journal. The three team hooks stay installed for environments where they fire, but nothing depends on them. board.yaml was already the source of truth, so the department still works. Recorded as C-015 and C-016.
2026-09-17 | P3b | Owner confirmed `wt -w 0 split-pane` opens in the current window, so the surface is VERIFIED and `dept up --surface` now runs it as well as printing it. A failure there is reported and never fatal: the seats are already working and a terminal that did not split is cosmetic.
2026-09-17 | P3 | PHASE 3 DONE. 283 tests pass.
2026-09-17 | P4 | All eight commands built: upgrade, memory show/compact, worktree create/remove, export plugin, meeting, cost, doctor --score, import bmad.
2026-09-17 | P4 | `memory compact` archives rather than deletes. MEMORY.md is preloaded as the first 200 lines or 25 KB, so a file that grows past that silently loses its own beginning; compaction moves the overflow beside it and tells the seat to promote anything that still matters.
2026-09-17 | P4 | `cost` counts sessions delphi started, not money, and says so in its own output. Phase 0 proved there is no stable local source for account spend.
2026-09-17 | P4 | `import bmad` reads the user's own PRD, epics, stories and sprint status. It copies no prompts or templates from that project.
2026-09-17 | P4 | Three defects found by running it: `export plugin --out <absolute path>` joined the path onto the project root and produced nonsense; the BMAD section regex was missing its `m` flag so every imported brief came out empty; and the import produced two board rows for one story when it appeared in both a document and the sprint status.
2026-09-17 | P4 | 305 tests pass.
2026-09-17 | P5 | MCP server built and verified three ways: 16 protocol unit tests, a real JSON-RPC exchange over stdio (handshake, tools/list returning 8 tools, tools/call task_list returning live ledger rows), and registration through `claude mcp add`, which correctly showed the project-scoped server as pending approval. The scratch registration was removed afterwards.
2026-09-17 | P5 | `delphi loop` built. The decisions are a pure state machine in core with 18 tests; the command is the spending. It does nothing without `--yes`: run bare it prints the ceiling in sessions, the first seat it would start and the exact prompt that seat would get.
2026-09-17 | P5 | The loop counter was wrong on the first pass. It counted review rounds, so a seat that stopped without touching the board left the next decision identical to the last one and the loop dispatched the same story forever. It now counts every dispatch, which is the honest bound because every dispatch costs the same. maxStories x maxAttemptsPerStory is the most sessions a run can ever start, and a test pins it.
2026-09-17 | P5 | Running it found C-017, which reading never would have: every background dispatch was broken on Windows. `claude` on PATH is an npm `.cmd` shim, `cmd.exe` treats a line break as a command separator, and every spawn prompt is multi-line, so execa refused the call before the session was created. This had been true since Phase 2 for `dept up`, `dispatch` and `meeting` as well. The adapter now resolves the shim to the `.exe` it names; verified against the installed claude.exe with a real multi-line argument.
2026-09-17 | P5 | Loop stop conditions exercised end to end against a stand-in binary: a seat reporting BLOCKED, a seat waiting on a permission prompt, a story that never moves, and a story the board blocks. All four stop, and the run says what it spent. 345 tests pass.
2026-09-17 | P5 | Companion extension built: `packages/vscode`, delphi-team-vscode. The board in the status bar, amber when something is blocked; one editor terminal per running seat, in columns; `delphi watch --follow` beside the code; one notification when a department starts.
2026-09-17 | P5 | It starts nothing. `dept up` creates the seats and this shows them, because dispatching costs quota and belongs in the command you typed, not in an editor reacting to a saved file.
2026-09-17 | P5 | There is no channel from the CLI to VS Code, and none is wanted: sessions.log is the channel. A second one could disagree with the board. `--surface vscode` therefore runs no command and says the extension will pick it up.
2026-09-17 | P5 | Each pane runs the session as its own process via shellPath/shellArgs rather than typing a line into a shell, so a path with a space in it needs no quoting and the pane closes when the seat does. Panes are only opened for sessions `claude agents --json` still lists: a log line records that a session was started, not that it still exists.
2026-09-17 | P5 | Verified by loading the built bundle with `vscode` stubbed: activation, the status bar, both commands, three panes for three live seats, and no pane for the one that had ended. PHASE 5 DONE. 345 tests pass.
2026-09-17 | P5 | Handoff written for a second machine. `.build/HANDOFF.md` carries the whole context a cold session needs: what is being built, who decides what, the rules that are not the agent's to change, where the build stands, the machine to set up, and every trap already paid for. `docs/try-it.md` is the hands-on guide with the quota-spending commands marked apart from the free ones.
2026-09-17 | P5 | The loop test rig moved into the repository as `scripts/fake-claude.mjs` and `scripts/loop-lab.mjs`. It lived in a scratch directory, which meant the only free way to exercise the one command that spends quota would have been lost. `node scripts/loop-lab.mjs` now builds a scratch project and runs five stop conditions plus the "without --yes it spends nothing" guard, from a clean checkout.
2026-09-17 | P5 | Writing the guide found a wrong claim in it before anyone read it: `task move ready -> done` is refused by the transition rule, not by the missing report. The report rule fires from `review`. Corrected.
2026-09-17 | P5 | README still said the CLI did nothing but report its version, which had been false since Phase 2. Fixed, and the command count in STATE corrected from 20 to the real 32.
2026-09-17 | P5 | Installed the CLI globally from the npm tarball rather than a link, which checks the published artifact at the same time: 80 KB, four files, core bundled in, dependencies resolve, `delphi` on PATH. That also matters because the generated settings.json calls a bare `delphi hook <event>` -- without delphi on PATH the hooks fail open and silently do nothing.
2026-09-17 | P5 | Setting a demo project up found a false deny in the PreToolUse gate: the allow-list matched `/.delphi/` with a leading separator, so an absolute path passed and a relative one did not. The orchestrator was refused its own ledger. The path is rooted before matching now, the decision is an exported function, and it has a test. A gate that wrongly allows costs a tidier diff; one that wrongly denies costs the shift.
2026-09-17 | P5 | Found by walking the guide rather than writing it -- and found the second way too: the first attempt to test the gate used `echo` with backslashes in the JSON, which mangled the payload, so the hook silently did nothing and looked broken. `printf '%s'` instead. Same trap already recorded in memory, walked into anyway.
2026-09-17 | P5 | The owner ran a real department on a real project, and the orchestrator itself reported two defects in `delphi watch` that no test had caught.
2026-09-17 | P5 | First: `watch` and `dept status` listed every background session Claude Code reports, not the ones delphi started. A leftover session from the Agent Teams spike appeared as a seat named after itself and marked blocked, which reads as a department in trouble when nothing of the sort is true. Both now join against sessions.log: a session delphi did not start is not a seat.
2026-09-17 | P5 | Second: `parseEntry` accepted any line with four pipe-separated fields, and the journal template explains its own format on a line that has exactly four. It was read as an event and `watch` printed "SO ti" where the time goes -- characters 11 to 16 of "Format: `<ISO time>". A line now has to start with a timestamp. `readJournalTail` also filters before slicing, so asking for eight events gives eight events rather than eight lines of which some were prose.
2026-09-17 | P5 | An existing test had used `timestamp: 't'` as a placeholder, which the new rule rejects. The rule is right and the fixture was never realistic; it now uses a real timestamp.
2026-09-17 | P5 | Panes now open for every way a seat is started, not just `dept up`. `dispatch` and `loop` had none, so a seat started by either worked, finished and told you afterwards through the ledger. The running of a surface is one helper the three share.
2026-09-17 | P5 | `surface` defaults to `auto`, which was `none`. auto splits what you are already sitting in: tmux inside tmux, the companion extension inside VS Code, Windows Terminal otherwise. The spec's own example config said `wt`; shipping `none` was drift.
2026-09-17 | P5 | The obvious probe for Windows Terminal is wrong, and finding out cost a round. Installed from the Store, `wt.exe` is an app execution alias -- a reparse point Node cannot stat. `existsSync` says false, `lstatSync` and even `readdirSync` on the directory say ENOENT, and `where wt` finds it happily. So auto does not probe: running `wt` is the test, it was already non-fatal, and it now fails with a line naming Windows Terminal rather than a bare ENOENT.
2026-09-17 | P5 | Then the panes opened and could not launch: `[error 0x80070002] when launching \`claude attach ...\``. Windows Terminal starts the pane command itself and does not resolve PATHEXT, so a bare `claude` -- the npm `.cmd` shim -- is not found. The same root as C-017, one layer out. Panes now launch the resolved `.exe`, and the dry run prints the same thing the real run would use.
2026-09-17 | P5 | `scripts/loop-lab.mjs` now passes `--surface none`. It dispatches eight times across its cases, and with panes on by default a test run carpeted the screen in terminals -- which is how the launch failure above was found.
2026-09-17 | P5 | Fourth finding from the same live run, reported by the orchestrator itself: a team can name a seat the project never built. `quick-fix` has a reviewer; a project scaffolded from `feature` does not, so the plan held it as "nothing assigned yet" and the review step went missing without anyone noticing. `dept up` now warns, and blocks when work is actually assigned to the missing seat.
2026-09-17 | P5 | A finished seat blocked the whole department, and only a second live run showed it. A background session that has completed its turn sits at `state: blocked, status: idle` -- it is not `done`, which happens only once it is stopped. The active-limit filter was `state !== 'done'`, so every seat that had ever run still counted, and with quick-fix's limit of two the next story could never start: "1 seats have work but only 0 can start". Active now means working: `state: working` or `status: busy`.
2026-09-17 | P5 | The same word was misreported to the reader. `watch` and `dept status` printed Claude Code's `blocked` for a seat that had simply finished, directly beside a board saying nothing was blocked. A contradiction like that is resolved by distrusting the view. Both now say working, waiting, or idle.
2026-09-17 | P5 | Found while setting up a second story for the owner to test with -- the setup itself was the test.
2026-09-17 | P5 | Pane layout is now main-vertical on both surfaces: you keep the left half and the seats stack down the right. Splitting without naming an axis left it to Windows Terminal, which halves whichever side is longer -- fine for two panes, a scattered grid by four, and never the same arrangement twice. The first split is `-V --size 0.5`; each one after is `-H` against the pane just created, with the share counting down (3/4, 2/3, 1/2) so every seat ends the same height. tmux uses `select-layout main-vertical` rather than `tiled`, which gave every seat the same weight as the pane you work in.
2026-09-18 | P5 | The owner's live test produced one finding worth more than the rest: a seat destroyed another seat's work, and delphi had every check needed to prevent it and ran none of them. `delphi dispatch` skipped the whole pre-flight that `dept up` runs, because the checks lived inside `planDepartment`. Both stories claimed `src/public/index.html` and both were dispatched anyway.
2026-09-18 | P5 | Fixed at the root rather than the symptom: `checkStories()` is a core function now, `dept up` and `dispatch` both call it, and `dispatch` refuses with both story ids and the file named. `--force` exists for someone who has already decided. Replaying yesterday's exact command against the fix: it is refused.
2026-09-18 | P5 | Second gap in the same area: the in-flight window was `ready` and `doing` only, so a story in `review` had its files treated as unclaimed while its seat was still live. `review` counts now.
2026-09-18 | P5 | PROTOCOL section 3 gains the rule the incident was actually about -- deleting is editing and cannot be undone; never remove a path you do not own; learn what a destructive command does in a scratch directory. Recorded as C-018, including what is still not covered: a hook sees `file_path` for Edit and Write, not what a shell command will unlink, so `delphi worktree` remains the answer when this must be impossible rather than merely refused.
2026-09-18 | P5 | Second item from the same live run, smaller but paid on every dispatch: every seat is told to read `docs/project-context.md` first, and on that project it was still the shipped template. Two seats read angle brackets, worked from the story alone, and said so in their reports; nothing had warned anyone. `checkProjectContext()` counts the unfilled lines and both dispatch paths warn. A warning and not a block -- a department works without it, it just works with less.
2026-09-18 | P5 | Walked into the Python replacement-string trap twice in one turn, on the very file that documents it: `\n` written into a patch becomes a real line break, and it broke both `department.ts` and a test file. The memory note says use the Write tool. Use the Write tool.
2026-09-18 | P5 | Third item from the live run, the quiet one: every git command during the test printed "LF will be replaced by CRLF". delphi writes the ledger with LF on every platform so a board reads the same on two machines, but git on Windows defaults to checking it back out as CRLF -- and then the next appended line is LF, the file is mixed, and the whole ledger reports as changed although nobody edited it. `delphi init` now merges a block into `.gitattributes` pinning `.delphi/**` and `.claude/**` to LF, which is what this repository already does for itself.
2026-09-18 | P5 | The block goes *above* whatever the user wrote, not below: git takes the last matching rule, so appending would silently override a choice they had made. Scope stops at the two directories delphi rewrites -- CLAUDE.md and docs/project-context.md are written once and then belong to the user, and a one-time conversion there is harmless. 370 tests pass.
2026-09-18 | P5 | `confirm_before_dispatch` is real now rather than removed. The spec declares it, so implementing it needed no amendment, and a config that promises a gate it does not have is worse than no config. `dept up` and `dispatch` ask before starting seats; `-y` skips. The part that decides the design: the orchestrator runs these through its Bash tool with no terminal on the other end, so a session that cannot be asked is not asked -- it proceeds and prints "not a terminal, so nothing could be asked", which is the difference between "you approved this" and "nobody could be asked".
2026-09-18 | P5 | Two display faults found while testing that: `dept status` showed the same seat twice with the same task, and still said `blocked` for a session that had finished. The session log was keyed by seat name, so two sessions for one seat both got whichever task was logged last -- keyed by session id now. And `status` is often absent entirely, so the earlier `status === 'idle'` fix never fired: `state: blocked` with no `waitingFor` is a finished turn, and `waitingFor` is the only field that means a seat wants a person.
2026-09-18 | P5 | Cost of testing this one honestly: the non-interactive path had to be exercised against the real binary, which started a session. Stopped immediately. `--dry-run` returns before the confirmation, so there was no free path through it.
2026-09-18 | P6 | PHASE 6 STARTED, approved by the owner. First piece: `delphi watch --web`. Same read-only view as the terminal one, on localhost, with the board, the seats, the reports and the assets. No framework and no build step -- `node:http`, one page as a string, polling one JSON endpoint.
2026-09-18 | P6 | Read-only is the design and not a limitation: GET only, no endpoint writes, and the board is still changed with `delphi`. Bound to 127.0.0.1 and never 0.0.0.0, because a board names people, blockers and the paths being worked in. Every served path is checked to be inside the ledger or the asset directory, with a test covering `..`, an absolute path elsewhere, and a sibling directory whose name merely starts the same way.
2026-09-18 | P6 | Driving the page found what reading it did not: the five-second refresh rebuilt the report list, so a report you had opened snapped shut and threw away the markdown it had fetched. Only visible if you open one and then wait. The list is rebuilt only when the set of reports actually changes now, and an open report survived three refreshes in the browser.
2026-09-18 | P6 | A security review of the pushed commit found a real hole in `watch --web`: DNS rebinding. Binding to 127.0.0.1 is not the defence I took it for -- any page in the user's browser can point a hostname it controls at 127.0.0.1 and then read this server from its own origin, because the browser is the thing on this machine. The bind address is satisfied; the ledger is not private. The `Host` header is the part an attacker cannot forge, so every request is now checked against the names this server actually answers to. Verified with a request carrying an attacker hostname: refused, including for a report path.
2026-09-18 | P6 | Three packs: solo-dev, software-team, content-team, with `delphi pack list | show | apply`. A pack is the answer to "which seats do I actually need", which a new project cannot answer and an experienced one answers differently every time.
2026-09-18 | P6 | A pack edits a file the user owns, so `apply` prints the diff and stops; `--write` is the second word. The merge goes through `yaml`'s document API rather than parse-and-stringify, because the shipped config is two thirds comments and those comments are the part that explains the numbers. Verified on a real project: 31 comments survived, the values landed, and applying twice is a no-op.
2026-09-18 | P6 | The first version of the pack command achieved nothing, and only running it showed that. It wrote role and team files into `.delphi/roles/` and `.delphi/teams/` -- which `readRole` and `readTeam` do not read. They read the embedded templates. So a pack's roles and its team now ship as ordinary templates and a pack is config and nothing else.
2026-09-18 | P6 | Same run found `role build --all` only rebuilding agents that already existed, so a pack could add three seats to the config and `--all` would build none of them. It means every seat the project has now: the ones built and the ones the config names.
2026-09-18 | P6 | Worth a later look, not touched here: `.delphi/roles/*.md` is written by `init` and read by nothing. `build.ts` documents it as "the package's base role, from `.delphi/roles/<base>.md`", which is not what the code does. A user editing one of those files sees no effect at all.
2026-09-18 | P6 | `docs/traceability.md` maps all 25 requirements to where they are met, and says plainly which three are not. R11 is the honest one: Agent Teams does not work the way the spec's diagram shows, so delphi depends on none of it. R04 and R05 wait on Phase 7. R03 wants a comparison table and R16 wants spike 6, and neither blocks anything. Every number in that table was counted rather than remembered -- the first draft said 12 standard roles and there are 10.
2026-09-18 | P6 | Docs site is markdown under `docs/` for GitHub Pages, as the owner chose: no generator, no build step, no CI job, and it reads the same on GitHub as on the web. `docs/README.md` is the index, `docs/writing-a-role.md` is the contribution guide for roles and capabilities, `examples/README.md` covers the packs. A link checker over the four new documents found none broken.
2026-09-18 | P6 | PHASE 6 DONE. 384 tests pass.
2026-09-18 | P7 | Bilingual documentation, English and Vietnamese, in pairs with a language switcher at the top of each: the npm page (`packages/cli/README.md` and `.vi.md`), the guide (`docs/guide.md` and `.vi.md`), and the docs index. The npm README is the full manual -- install, five minutes, every command marked free or quota-spending, config, safety, troubleshooting. The guide is the part the command reference cannot carry: the mental model, the anatomy of a seat, a story as a contract, a shift end to end, and what each safety rule costs.
2026-09-18 | P7 | npm only ships `README.md` by default, so the Vietnamese one is named in `files` and both are in the tarball. The first pass had the docs index language switcher pointing at the guide rather than a Vietnamese index; caught by reading it back, and `docs/README.vi.md` now exists.
2026-09-18 | P7 | Every relative link across the nine documents is checked mechanically rather than by eye.

